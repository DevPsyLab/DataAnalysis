---
title: "jsPsych"
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  error = TRUE,
  comment = "",
  class.source = "fold-show")
```

# Troubleshooting

1. Press `F12` to Open Chrome Developer Tools
1. Switch to the `Sources` tab
1. Press `F8` when you want to pause execution
1. Then, you can run commands in the console
